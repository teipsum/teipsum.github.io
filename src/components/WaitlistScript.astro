---
// The waitlist form's script, shared by both languages so it ships as one file.
// It reads the page language for its messages.
---

<script>
  import { initWaitlist } from '../scripts/waitlist';
  initWaitlist();
</script>
